general:
  name: joker
components:
  - type: stateless
    name: writer
    containers:
      - dir: ./writer
    expose:
      - servicePort: 80
        containerPort: 80
        external: true
  - type: stateless
    name: joker
    containers:
      - dir: ./joker
    expose:
      - servicePort: 80
        containerPort: 80
tests:
  - name: load
    containers:
      - dir: ./tests/load
  - name: func
    containers:
      - dir: ./tests/func
